/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ManaPipe } from './mana.pipe';

describe('Pipe: Manae', () => {
  it('create an instance', () => {
    let pipe = new ManaPipe();
    expect(pipe).toBeTruthy();
  });
});
